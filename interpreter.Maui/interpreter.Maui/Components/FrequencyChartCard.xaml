<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="interpreter.Maui.Components.FrequencyChartCard"
             x:Name="ThisControl">
    
    <Border Background="{Binding CardBackgroundColor, Source={x:Reference ThisControl}, FallbackValue=#111827}"
            StrokeThickness="1"
            Stroke="#1F2937">
        <Border.StrokeShape>
            <RoundRectangle CornerRadius="20"/>
        </Border.StrokeShape>
        <Border.Shadow>
            <Shadow Brush="#33000000" Offset="0,8" Radius="18" Opacity="0.3"/>
        </Border.Shadow>
        <Border.Padding>
            <OnIdiom x:TypeArguments="Thickness"
                     Phone="16"
                     Tablet="20"
                     Desktop="20"/>
        </Border.Padding>
        <Border.HeightRequest>
            <OnIdiom x:TypeArguments="x:Double"
                     Phone="130"
                     Tablet="150"
                     Desktop="150"/>
        </Border.HeightRequest>
        <VerticalStackLayout Spacing="12">
            <Label Text="{Binding Title, Source={x:Reference ThisControl}}"
                   TextColor="{Binding TitleColor, Source={x:Reference ThisControl}, FallbackValue=#22C7B8}"
                   FontAttributes="Bold">
                <Label.FontSize>
                    <OnIdiom x:TypeArguments="x:Double"
                             Phone="13"
                             Tablet="14"
                             Desktop="14"/>
                </Label.FontSize>
            </Label>
            <GraphicsView x:Name="ChartView" 
                          HeightRequest="100"
                          Drawable="{Binding ChartDrawable, Source={x:Reference ThisControl}}"/>
        </VerticalStackLayout>
    </Border>
</ContentView>
