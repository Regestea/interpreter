<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="interpreter.Maui.Components.SettingsMenuItem"
             x:Name="ThisControl">
    
    <Border BackgroundColor="Transparent"
            StrokeThickness="0"
            Padding="8">
        <Border.GestureRecognizers>
            <TapGestureRecognizer Tapped="OnTapped"/>
        </Border.GestureRecognizers>
        <HorizontalStackLayout Spacing="12">
            <Label x:Name="IconLabel"
                   Text="{Binding Icon, Source={x:Reference ThisControl}}"
                   VerticalOptions="Center">
                <Label.FontSize>
                    <OnIdiom x:TypeArguments="x:Double"
                             Phone="18"
                             Tablet="20"
                             Desktop="20"/>
                </Label.FontSize>
            </Label>
            <Label x:Name="TextLabel"
                   Text="{Binding Text, Source={x:Reference ThisControl}}"
                   TextColor="{Binding TextColor, Source={x:Reference ThisControl}, FallbackValue=White}"
                   VerticalOptions="Center">
                <Label.FontSize>
                    <OnIdiom x:TypeArguments="x:Double"
                             Phone="13"
                             Tablet="14"
                             Desktop="14"/>
                </Label.FontSize>
            </Label>
        </HorizontalStackLayout>
    </Border>
    
</ContentView>
